# Concepts Ledger (safety precaution #5)

Running record of concepts, libraries, and functions **first introduced** on each
day. Purpose: enforce the rule that a day may only use what earlier days have
already taught — no concept before it's introduced.

**Update this every time a day is authored**, and read it before authoring a new
day (the adversarial review, #3, checks new days against it).

| Day | First introduced |
|-----|------------------|
| 001 | AI pipelines; `edge_tts.Communicate`; async/await; `subprocess`; `subprocess.run(..., capture_output=True, text=True)`; FFmpeg (via subprocess); `json.loads` (pipeline context — full json treatment in Day 002); "black-box model" concept; slides→audio→talking-head→composite flow |
| 002 | functions (`def`, params, `return`, defaults); type hints; docstrings; string methods (`.strip/.lower/.upper/.replace/.split/.join`); f-strings + format specs; string immutability; `' '.join(text.split())` whitespace-collapse idiom; lists; dicts; `.get(k, default)`; `for` loops; list & dict comprehensions (+ `if` filter); files (`open`/`with`, `'r'`/`'w'`); `json` (`dump`/`load`/`dumps`/`loads`) — full treatment; `try`/`except` (`FileNotFoundError`, `JSONDecodeError`); Load→Clean→Validate→Save pattern |
| 003 | `ollama` runtime (`pip install ollama`); `ollama.chat(model, messages)`; `response["message"]["content"]`; `openai` package (`pip install openai`); `OpenAI(base_url="http://localhost:11434/v1", api_key="ollama")`; `client.chat.completions.create()`; `response.choices[0].message.content`; `requests.post()` for raw HTTP; `POST http://localhost:11434/api/chat`; `"stream": False` (Ollama defaults to streaming NDJSON); `response.raise_for_status()`; `response.json()`; `{"role": "system"/"user"/"assistant", "content": "..."}` message format; system prompt as first message in the list; multi-turn history as a growing messages list; `list[dict]` / `tuple[str, list[dict]]` subscript type hints (in scaffold — full generic syntax in Day 004); `input()` built-in; `while True:` loop; `break`; `continue` |
| 004 | `pydantic` (`pip install pydantic`); `BaseModel`; `Field(description=...)`; `model_validate(dict)` (v2 — not `parse_obj`); `model_validate_json(str)` (v2 — not `parse_raw`); `model_json_schema()` (v2 — not `.schema()`); Pydantic v2 schema output includes `"title"` on every property; `ValidationError`; `e.errors()` (list of error dicts); `list[str]` built-in generic type hint (Python 3.9+ — older code uses `List[str]` from `typing`); `format="json"` in `ollama.chat()` (JSON mode — grammar-constrained sampling); `json.dumps(schema, indent=2)` for schema→prompt serialisation; schema-guided system prompt pattern; schema-echo failure mode (model returns schema structure instead of filled instance — fix: add concrete example to system prompt); retry loop (`for attempt in range(max_tries)`); self-correction via appending `ValidationError` message as a user turn |
| 005 | `import logging`; `logging.basicConfig(level=..., format=...)` (configure once); `logging.getLogger(__name__)` (named module logger); `logger.debug/info/warning/error/exception(msg, *args)` (five levels — percent-style args, not f-strings); log level hierarchy (DEBUG < INFO < WARNING < ERROR < CRITICAL); `%r` repr format in log messages; multiple `except` clauses on one `try`; bare `raise` (re-raise preserving original traceback); `logger.exception()` (logs ERROR + full stack trace); fallback return pattern (`if fallback: return fallback; raise`); `X \| None` union return type (Python 3.10+ syntax — equivalent to `Optional[X]` from `typing`); `requests.exceptions.Timeout/HTTPError/ConnectionError` (illustrative only — exception submodule of `requests`, introduced in Day 3); `pytest` (`pip install pytest`); `def test_*()` naming convention; `pytest.raises(ExceptionType)` context manager; calling test functions directly in-notebook; `globals()` (kernel namespace check in notebooks); temporary global patching with `try/finally` for error-path tests |
| 006 | four-part prompt anatomy (instruction / context / input / output format); system prompt as standing instructions (`{"role": "system", "content": "..."}` — first message, applies to every response); role/persona pattern (role + tone + constraints + output format in system prompt); `make_system_prompt(role, constraints, output_style) -> str` builder pattern; few-shot prompting — 2–5 worked (input, output) examples before the real user message; zero-shot vs one-shot vs few-shot terminology; example turns as alternating `user`/`assistant` messages in the messages list; `build_few_shot_messages` helper pattern (loop over example tuples, append user+assistant pairs, append real input); output format constraints — technique 1: explicit format instructions (name format, specify structure, prohibit preamble); output format constraints — technique 2: sentinel/prefix trick (end user message with opening token of desired format, e.g. `"Your JSON:\n{"`); prompt library module pattern — named builder functions each returning `list[dict]`, collected in a dispatch dict; separation of prompt construction from model calls (builders only construct; callers call `ollama.chat()`); single-entry-point dispatcher (`apply_transform(name, text) -> str`) as the public interface to a prompt library |
| 007 | context window concept (hard token limit; silent truncation vs hard error); `CONTEXT_WINDOW_TOKENS`, `CHARS_PER_TOKEN`, `PROMPT_OVERHEAD_TOKENS`, `MAX_DOC_TOKENS`, `MAX_DOC_CHARS` constants pattern; `estimate_tokens(text: str) -> int` using `len(text) // CHARS_PER_TOKEN` (integer-division approximation); `will_fit(text: str) -> bool` (compare `estimate_tokens` to `MAX_DOC_TOKENS`); chunking — fixed-size slices with overlap; `chunk_text(text, chunk_size, overlap) -> list[str]` (while-loop cursor advancing by `step = chunk_size - overlap`); `overlap >= chunk_size` guard raises `ValueError`; Map-Reduce pattern for LLMs (map one model call per chunk; reduce with one final synthesis call); `summarize_chunk(chunk, model) -> str` (atomic map unit: system prompt + `ollama.chat` + return content string); `map_summaries(chunks, model) -> list[str]` (loop over chunks, collect per-chunk summaries); `reduce_summaries(summaries, model) -> str` (`"\n\n".join(summaries)` separator, synthesis-focused system prompt, one `ollama.chat` call); `summarize_document(text, chunk_size, overlap, model) -> str` (full pipeline: will_fit fast-path or chunk→map→reduce); module-level system prompt constants (SUMMARIZE_SYSTEM_PROMPT, REDUCE_SYSTEM_PROMPT) |
| 008 | Zero-shot classification (LLM picks one label from a fixed set; no training data needed); classification system prompt anatomy (enumerate labels, one-word reply constraint, no prose); label validation pattern — `.strip().lower()`, membership check `[lb.lower() for lb in labels]`, raise `ValueError` on invalid label; `classify(text, labels, model) -> str` (single-label classifier); `model_dump()` (Pydantic v2 — converts `BaseModel` to plain `dict`; complements `model_validate_json` from Day 4); `classify_with_confidence(text, labels, model) -> dict` (schema-guided classifier: `ClassificationResult` BaseModel + `model_json_schema()` in prompt + `format="json"` + `model_validate_json` — Day-4 Pydantic pattern applied to classification with label + reasoning + confidence); multi-label classification (text belongs to more than one category; model returns JSON array); `classify_multi_label(text, labels, model) -> list[str]` (JSON-array parsing with CSV fallback; case-insensitive validation; dedup with `seen: set[str]`); `set[str]` built-in generic type hint (Python 3.9+, same syntax as `list[str]` from Day 4); error-safe batch loop — `try/except Exception` inside `enumerate`, per-item failure recorded as error dict, loop never stops; result dict convention `{"text", "label", "status": "ok"\|"error", "error": str\|None}` (four fixed keys); `classify_batch(texts, labels, model) -> list[dict]`; `sum(1 for r in results if ...)` generator expression for counting (distinct from list comprehension) |
| 009 | Nested Pydantic models (one `BaseModel` subclass as a field type in another — e.g. `address: Address \| None`); optional Pydantic fields: `field: SomeType \| None = Field(default=None, description="...")` (Day 5's `X \| None` syntax applied to Pydantic); `default_factory=list` for list fields in Pydantic (`Field(default_factory=list)`) — never `default=[]` (mutable default pitfall); `type[BaseModel]` annotation — the class object itself, not an instance, used when a function accepts any Pydantic schema class; Define-Prompt-Validate three-step as a named extraction pattern (define schema → inject `model_json_schema()` into system prompt → validate with `model_validate_json()`); `messages_override: list[dict] \| None` parameter for optional few-shot injection into extraction functions; `list(messages_override)` defensive copy before retry loop to prevent mutating the caller's list; section-aware extraction: `split_sections(doc, section_names) -> dict[str, str]` (line scanner — header line triggers section switch; pre-heading text stored under `'PREAMBLE'`; uppercase section names as dict keys); `{label: "\n".join(lines).strip() for ...}` dict comprehension to filter empty sections; assembling a top-level result model from per-section extraction objects |
| 010 | string.Template (stdlib — no install needed); Template('...') constructor; $variable and ${variable} placeholder syntax; Template.substitute(mapping_or_kwargs) — raises KeyError on missing key; Template.safe_substitute(mapping_or_kwargs) — leaves missing placeholders unchanged; Template.template attribute (the raw template string); import re; re.findall(pattern, string) — returns list of all non-overlapping matches; placeholder detection pattern r'\$\{?(\w+)\}?' for extracting variable names from template strings; dataclasses.dataclass decorator (stdlib); @dataclass; dataclasses.field(default_factory=list) — same semantics as Pydantic's default_factory from Day 9, here in stdlib dataclasses; PromptTemplate dataclass pattern (name: str, template_str: str, description: str, required_vars: list[str]); auto-detecting required_vars from template string at __post_init__; __post_init__ method in dataclasses (runs after __init__, used for derived-field computation); prompt registry as dict[str, dict] keyed by 'name_vN'; json.dump(registry, f, indent=2) for saving registry to file (extends Day 2 json treatment); json.load(f) for loading registry (Day 2 — re-applied to registry persistence); 'never mutate a version' convention (append _v2, _v3 instead); render-then-call pattern (PromptTemplate.render(variables) -> str produces filled user message; result passed to ollama.chat()); separation of prompt construction from model invocation as an architectural principle |
| 011 | embeddings (dense vector representation of text meaning; fixed-length list[float]); embedding model vs chat model (two distinct Ollama model types; e.g. nomic-embed-text for embeddings vs llama3.2 for chat); ollama.embeddings(model: str, prompt: str) -> dict — returns {'embedding': list[float]}; response['embedding'] (the vector extracted from ollama.embeddings result); vector / embedding dimensionality (length of the list[float]; fixed per model); cosine similarity formula: dot(a,b) / (|a| * |b|); range [-1, 1]; 1 = identical direction; dot product: sum(x*y for x, y in zip(a, b)); vector magnitude: math.sqrt(sum(x**2 for x in v)); import math; math.sqrt(); semantic search index: list[dict] with keys 'text' and 'embedding' pre-computed at index time; index-time vs query-time embedding (documents embedded once; only the query is embedded per search); O(n) brute-force search: embed query, compute cosine similarity against all index entries; top-k retrieval: sorted(results, key=lambda r: r['score'], reverse=True)[:top_k]; sorted() with key= and reverse=True for descending sort; zip(a, b) for parallel iteration over two equal-length sequences (used in dot product) |
| 013 | RAG (Retrieval-Augmented Generation) pipeline concept: retrieve → augment → generate; `build_rag_prompt(question: str, chunks: list[str]) -> str` (join chunks with `"\n\n---\n\n"`, format as Context/Question/Answer); GROUNDING_SYSTEM_PROMPT pattern ("answer only from context, say I don't know if not present"); chunk_document for retrieval (sliding-window chunker, same algorithm as Day 7's chunk_text — applied to create independently searchable units rather than summarisation slices); index_corpus ingestion pipeline (chunk → embed with nomic-embed-text → collection.add with chunk_id f"{source}_{i:04d}" and metadata {source, chunk_index}); retrieve_context(question, collection, top_k) → list[str] (embed question, collection.query(query_embeddings=[...], n_results=top_k), return result["documents"][0]); grounding discipline (system prompt forbids model from going beyond retrieved context); "I don't know" path as a correctness feature; index-time vs query-time cost asymmetry (index once, query many times) |
| 012 | chromadb (pip install chromadb); chromadb.Client (in-memory client — chromadb.Client()); chromadb.PersistentClient(path=str) (disk-backed client); client.create_collection(name: str) -> Collection; client.get_collection(name: str) -> Collection; client.get_or_create_collection(name: str) -> Collection; Collection.add(ids: list[str], embeddings: list[list[float]], documents: list[str], metadatas: list[dict]); Collection.query(query_embeddings: list[list[float]], n_results: int, where: dict | None) -> dict; Collection.get(ids: list[str] | None) -> dict; Collection.upsert(ids, embeddings, documents, metadatas) — add-or-replace semantics; Collection.delete(ids: list[str]); Collection.count() -> int; Chroma where-clause filter syntax: {'key': {'$eq': val}}, {'$in': [...]}, {'$gte': val}; approximate nearest-neighbour (ANN) indexing concept; vector database concept (persistent, indexed store for embeddings); metadata as searchable attributes stored alongside each vector; EmbeddingFunction interface (chromadb.EmbeddingFunction base class); index-time vs query-time separation (documents embedded once; queries embedded per search — reinforced from Day 11 in a persistent context) |
| 014 | `retrieve_with_sources(question, collection, top_k) -> list[dict]` — zip `results["documents"][0]`, `results["metadatas"][0]`, `results["distances"][0]` into dicts with keys `{text, source, chunk_index, distance}`; ChromaDB L2 distance as a relevance signal (lower = more similar); `filter_by_relevance(sources, max_distance) -> list[dict]` — list comprehension on `source["distance"] <= max_distance`; empty-result path as a feature (skip LLM call, return "I don't know"); citation-numbered prompt pattern — `build_cited_prompt(question, sources) -> str` (enumerate sources 1-based, prefix each `[N] (filename)\ntext`); `CITED_SYSTEM_PROMPT` with inline citation instruction; `extract_citations(answer, sources)` — `re.findall(r'\[(\d+)\]', answer)` to parse `[N]` references; sentence-aware chunking — `split_sentences(text)` using `re.split(r'(?<=[.!?])\s+', ...)` lookbehind regex; `chunk_by_sentences(text, max_chars, overlap_sentences) -> list[str]` — accumulate whole sentences, flush on `len(candidate) > max_chars`, seed next chunk with `(current[-overlap_sentences:] if overlap_sentences else []) + [sent]`; Python gotcha: `list[-0:]` == `list[0:]` (full list) — always guard before negative-zero slice; LLM-as-judge evaluation pattern — `RagEval` Pydantic model (`faithfulness`, `relevance`, `verdict`, `reason`); concrete filled-JSON example in system prompt as anti-schema-echo technique (Day 4 reinforcement); `evaluate_rag_answer(question, answer, context_chunks, model) -> RagEval`; faithfulness (claim in context?) and relevance (answers the question?) as the two core RAG metrics; PASS threshold = both scores >= 0.7 |
